#include <iostream>
#include <vector>
using namespace std;

int pivotIndex(vector<int> nums) {
    
}

int main() {
    vector<int> nums = {1,2,3,3,0};
    cout << "Pivot Index = " << pivotIndex(nums);
}
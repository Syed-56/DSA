#include <iostream>
#include <vector>
using namespace std;

int profit(vector<int> nums) {
    int n = nums.size();
}

int main() {
    vector<int> nums = {1,2,3,5,8};
    cout << "Profit: " << profit(nums);
}